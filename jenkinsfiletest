properties([[$class: 'GithubProjectProperty', displayName: '', projectUrlStr: 'https://github.com/bitnami/'], parameters([[$class: 'GitParameterDefinition', branch: '', branchFilter: 'master', defaultValue: '', description: 'Release to build', name: 'Version', quickFilterEnabled: true, selectedValue: 'NONE', sortMode: 'NONE', tagFilter: '*-r*', type: 'PT_TAG']]), pipelineTriggers([])])

pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building.. yeah!'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
