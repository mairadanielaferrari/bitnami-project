pipeline {
    agent {
        label 'docker'
    }
    environment {
        test = "test"
    }
    
    options {
        disableConcurrentBuilds()
        skipStagesAfterUnstable()        
    }
    
    parameters {
        string(name:'repository', defaultValue: '${repository}', description: 'Git Repository')        
    }
    
    stages {        
        stage('Checkout') {            
            steps {          
                git '${env.repository}'
                echo "step 1"
            }
            post {
            success {
                echo "success"
                   }            
            failure {                    
                echo "failure"
            }
           }
        }      
}
    }
