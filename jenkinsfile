pipeline {
    agent {
        label 'docker'
    }
    environment {
        repository = "${env.repository}"
    }
    
    options {
        disableConcurrentBuilds()
        skipStagesAfterUnstable()        
    }
    
    parameters {
        string(name:'repositoryyyy', defaultValue: '${repository}', description: 'Git Repository')        
    }
    
    stages {        
        stage('Checkout') {            
            steps {          
                git '${repository}'
                echo "step 1"
            }
            post {
            success {
                echo "success"
                   }            
            failure {                    
                echo "failure"
            }
           }
        }      
}
    }
